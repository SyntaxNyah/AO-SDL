global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  - job_name: kagami
    metrics_path: /metrics
    static_configs:
      - targets: ['kagami:80']
        labels:
          instance: canary
