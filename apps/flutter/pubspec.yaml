name: ao_mobile
description: Attorney Online 2 mobile client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^9.0.0

flutter:
  uses-material-design: true
