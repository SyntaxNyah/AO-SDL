#import <Flutter/Flutter.h>

/// Registers a native texture with the Flutter engine that displays
/// the courtroom scene rendered by IRenderer (Metal offscreen FBO).
@interface AoTexturePlugin : NSObject <FlutterPlugin>
@end
